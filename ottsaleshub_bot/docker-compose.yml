# Local run that behaves like production: docker compose up --build
services:
  bot:
    build: .
    env_file: .env
    ports: ["8080:8080"]
    volumes: ["./data:/data"]
    restart: unless-stopped
